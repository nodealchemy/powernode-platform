development:
  adapter: redis
  url: <%= ENV.fetch("ACTION_CABLE_REDIS_URL", "redis://localhost:6379/2") %>
  channel_prefix: powernode_cable

test:
  adapter: test

production:
  adapter: redis
  url: <%= ENV.fetch("ACTION_CABLE_REDIS_URL", "redis://localhost:6379/2") %>
  channel_prefix: powernode_cable
