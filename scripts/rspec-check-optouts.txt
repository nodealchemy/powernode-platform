# Extensions deliberately EXEMPT from the backend-spec phase of
# scripts/validate.sh.
#
# Format: <extension-slug><whitespace><reason>
# Lines starting with # are comments.
#
# An extension that ships *_spec.rb files and has NO line here is RUN. That is
# the point: absence used to be a silent skip, which is exactly how
# extensions/system's 608 spec files stayed outside the gate while it reported
# green — 21901 platform examples passing on 2026-08-05 while CI was red with 43
# extension failures. Both numbers were true; they were measuring different
# things.
#
# This mirrors scripts/tsc-check-optouts.txt, which exists for the same reason
# on the TypeScript axis ("marketing and supply-chain went unchecked for
# months"). Same hole, different language.
#
# Before adding a line here, prefer fixing the specs. An exemption is a decision
# you are asking a future reader to trust, so give a reason that survives being
# read six months from now — WHY it cannot run, and what would let it.
#
# To defer the extension suites for one run without exempting anything
# permanently, use `--skip-extension-specs`. It prints a warning every time.
trading  operator decision 2026-07-31 — extension is out of scope; it is already exempt from the tsc gate for the same reason (see scripts/tsc-check-optouts.txt). Remove this line if trading comes back into scope, and expect a backlog.
supply-chain  50 pre-existing failures, both causes environmental and neither introduced by the gate. (1) 41+ specs construct Devops::Repository, which declares no table_name so Rails infers "repositories" — a table that does not exist; the schema has git_repositories, used by Devops::GitRepository. The model is orphaned against a renamed table. (2) ~9 specs assert a MarketplacePublishable concern that exists nowhere in this checkout and is referenced only by these specs, never by supply-chain's own source. Tracked as its own work; 4301 examples otherwise pass. Remove this line when those two are resolved.
